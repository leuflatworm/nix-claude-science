# Nix build outputs
result
result-*

# Locally downloaded upstream binaries (never vendor these into the repo)
linux-x64
darwin-x64
darwin-arm64
operon-*

# Claude Code local session settings
.claude/settings.local.json
.claude/settings.local.json.tmp.*
